# Decisions log

Append one entry per decision. Format: date, work package, decision, reason, alternatives rejected.

## Open at start (decide in the WP indicated)

- **WP2** — `criteria` semantics for `RepositoryInterface::findBy/findOneBy`: equality, IN for lists, IS NULL for null. Confirm the domain uses nothing else; if it does, list it here.
- **WP2** — Default `EncrypterInterface`: libsodium XChaCha20-Poly1305 keyed from `Secrets`. Confirm key derivation is compatible with data encrypted by 1.0 (TOTP secrets), or provide a re-encryption path in `UPGRADE.md`.
- **WP3** — Identity map scope for `GenericRepository::persist` (per request vs per transaction).
- **WP4** — Reimplementation of the single `Cycle\Database\Injection\Fragment` usage per dialect.
- **WP5** — Meaning of the `suspended` validation rule (one occurrence) and whether it is validation or authorization.
- **WP5** — Whether `users/disable` stays `destructive` (see `effects.md`).
- **WP6** — Fixture normalisation rules (ids, timestamps, tokens, OTP codes) for the contract freeze.
- **WP1** — Which tests depend on Univeros wiring and are skipped until WP4/WP7 (keep the list here and empty it).
- **Any** — PHP minimum stays 8.3 (do not raise during extraction).

## Log

<!-- YYYY-MM-DD · WPn · decision · reason · rejected alternatives -->
- 2026-09-09 · WP0 · Root `composer.json` changes limited to `name` (`polaris/monorepo`), `description`, and `license` (MIT); `composer.lock` refreshed with `composer update --lock` (content-hash only, no dependency change). `keywords`, `homepage`, `support`, `type`, and the 1.0 autoload mapping stay until WP1 rewrites the root as the path-repository host. · Reason: WP0 is seed + rename; dependencies must not move before WP1. · Rejected: updating support URLs and keywords now (redone in WP1 anyway).
- 2026-09-09 · WP0 · No `LICENSE` file added; README "License" section (still "Proprietary") left untouched. · Reason: the copyright holder line is the owner's call, and README is the 1.0 Univeros-module README that gets rewritten when packages receive their own READMEs (WP1/WP8). `composer.json` is the licence source of truth meanwhile. · Rejected: guessing a copyright line; patching one README line in a document that is otherwise wrong for this repository.
- 2026-09-09 · WP0 · CI workflow kept as is: `composer validate --strict` plus the three separate `composer cs` / `composer stan` / `composer test` steps, rather than one `composer qa` step. · Reason: identical commands, clearer failure attribution in the Actions UI. · Rejected: collapsing into a single `composer qa` step (no gain).
- 2026-09-09 · WP0 · Each published package (`polaris/core`, `polaris/psr15`, `polaris/pdo`, `polaris/testing`, `polaris/cli`) gets its own read-only repository split from this monorepo, so a consumer requires one framework-agnostic package without pulling the monorepo or its siblings. This monorepo stays the single source of truth; nothing is committed to a split repository directly. · Reason: owner decision (2026-09-09); matches the "split-published" model in `coupling-analysis.md` and the per-package `v0.1.0` tags in `spec.md` §9. · Open for WP1: GitHub org and repository names, split tooling (subtree-split action vs. monorepo-builder), tag propagation. · Rejected: publishing from the monorepo root (Packagist needs one `composer.json` at each package repository's root).
- 2026-09-09 · WP1 · The seeded code keeps its `Univeros\Polaris\` namespace while moving to `packages/core/src`; each later WP renames the layers it ports to `Polaris\*`. · Reason: AGENT.md describes exactly this ("present until each layer is moved"); a wholesale rename is not in WP1's scope and would touch the 53 `api/*.yaml` class references that WP5 rewrites anyway. · Rejected: renaming everything in WP1.
- 2026-09-09 · WP1 · Each package manifest is truthful at every step: `packages/core/composer.json` requires the `univeros/*` packages its code still imports, and WP7 removes them when the last import goes. The root requires only the five `polaris/*` packages through one `packages/*` path repository, pinned to `0.1.x-dev` via the repository's `versions` option (path repositories otherwise guess the version from the current git branch); sibling packages require `polaris/core` as `^0.1`. · Rejected: parking the framework requirements in the root (core's manifest would lie about its own needs); Symfony-style `replace` at the root (duplicates every package's autoload mapping).
- 2026-09-09 · WP1 · `database/migrations` moves with core to `packages/core/database/` because `Module.php` resolves it relative to `src`; the whole directory is deleted in WP4 when `schema:export` replaces it. `api/` stays at the repository root per spec §1; nothing under `packages/` reads it until WP5.
- 2026-09-09 · WP1 · The forbidden-import check is `bin/check-imports`, a shell grep run by `composer imports` inside `composer qa` and by CI, reporting `Altair\`, `Cycle\`, the seeded `Univeros\Polaris\` namespace and any other `Univeros\` separately; advisory (exit 0) until WP5. · Reason: the spec allows a PHPStan rule or a grep; the grep also catches namespaces in strings and attributes, and needs no code. · Rejected: a custom PHPStan rule.
- 2026-09-09 · WP1 · Tests skipped because they need Univeros wiring: none. Nothing functional changed in WP1, so all 519 tests still run from `packages/core/tests`; the list is opened when WP2 or later has to skip something.
- 2026-09-09 · WP1 · `squizlabs/php_codesniffer` bumped to `^3.13.6` (dev-only) for CVE-2026-67434, the one advisory `composer audit` reported at WP0. No other dependency moves.
- 2026-09-09 · WP2 · `Polaris\Contract\RepositoryInterface` is `find`, `findOneBy`, `findBy`, `findAll` and `UnitOfWorkInterface` is `persist`, `remove`, `flush`: the split Altair has and the one the domain calls (persist 56, flush 42, remove 9 on the unit of work; save, delete, clear, transaction 0). Spec §3.1's sketch, which put `persist`/`remove` on the repository and added `transaction()`, is not followed. · Reason: "same method names and semantics the domain already uses"; zero service edits. `transaction()` arrives with the adapter in WP3 if the generic repository needs it. `findBy` keeps Altair's single-argument shape; ordering and limits are added in WP3 where the PDO path needs them. Criteria in use are equality, IN (list) and IS NULL only (verified by grep: no operator arrays).
- 2026-09-09 · WP2 · The ported layers (`Contract`, `Identity`, `Mfa`, `Token`, `Authorization`, `Security`, `Config`, `Event`, `Exception`, `Support`) take their `Polaris\*` namespaces now; `Contracts/` becomes `Contract/`. `Entity`, `Persistence`, `Http`, `Bootstrap`, `Module.php`, `Observability`, `Maintenance`, `Notification` and every test namespace stay `Univeros\Polaris\*` until their own WP (tests: WP7). WP2's acceptance line "nothing except Persistence/Http" is read as "nothing in the ported layers"; the unported layers are checked by their WPs.
- 2026-09-09 · WP2 · Univeros glue lives in `Bootstrap/` (deleted in WP7): `DualToken` (one object satisfying both token contracts, because the framework middleware stores the token on the request and the services read it), `AltairTokenFactoryBridge` (Polaris exceptions rethrown as the framework's so its middleware still answers 401/403), `AltairIdentityProviderBridge`, `UnitOfWorkBridge` (framework Cycle unit of work under the Polaris contract), and an Altair `RateLimit` conversion in `HttpBindings`. The 15 Cycle repositories implement the Polaris repository contract as well until WP3 replaces them. · Rejected: making Polaris exceptions extend the framework's (core may not import it).
- 2026-09-09 · WP2 · `TokenInterface` carries no `TOKEN_KEY` constant. · Reason: the request-attribute name is an adapter concern (WP5 `Input`, WP6 PSR-15), and two interfaces with a same-named constant of different values cannot be implemented by one class, which `DualToken` needs. `getMetadata(null)` returns all claims (Altair returned null; nothing called it without a key).
- 2026-09-09 · WP2 · `EncrypterInterface` is `encrypt(mixed): string` and `decrypt(string): mixed`; the AES constants and `hash()` are dropped (unused). Default `SodiumEncrypter`: XChaCha20-Poly1305 with a key derived from `Secrets::$appKey` by HKDF-SHA256 (context `polaris:encrypter:mfa`), payload `base64(version byte . nonce . ciphertext)`, scalars only. No compatibility with data encrypted by 1.0 is provided: `univeros/polaris` keeps its own AES-CBC encrypter, and nothing migrates from it to `polaris/core`. (Closes the WP2 open item.)
- 2026-09-09 · WP2 · Spec §3.5 ports: no `Polaris\ClockInterface` wrapper (services already take PSR-20 `ClockInterface`; `Polaris\Support\SystemClock` is the default). `RateStore` is introduced in WP6 with the ported rate-limit middleware, `Denylist` only if WP7 wiring needs one (the existing `AccessTokenDenylist` is per-user watermarks over PSR-16, not per-jti). Nothing in core does a framework cache or clock lookup.
- 2026-09-09 · WP2 · `CycleIdentityProvider` stays in core: it only uses the repository contract and the `User` entity, not Cycle. It is renamed in WP3 together with `Entity` → `Model`. `LcobucciTokenParser`, `TokenConfiguration` and the `Token` value object are copied into `Polaris\Token`; core requires `lcobucci/jwt`, `psr/clock` and `ext-sodium`.
- 2026-09-09 · WP2 · Skip list stays empty: all 519 tests plus the 6 new `SodiumEncrypter` tests run.
- 2026-09-09 · WP3 · Persistence contract split kept from WP2: `GenericRepository` reads (find, findOneBy, findBy, findAll; criteria use property names and are mapped to columns by the schema) and `Repository\UnitOfWork` writes. `flush()` is not a no-op: it writes the queued objects inside one adapter transaction, inserting what the identity map has not seen and updating only the columns that changed since the object was read or last written. · Reason: Cycle also wrote at flush time, so code that mutates an object between `persist()` and `flush()` keeps working; immediate writes would silently drop those changes. · Rejected: spec §4.4's no-op flush with immediate persist.
- 2026-09-09 · WP3 · Identity map: one `IdentityMap` per unit of work, shared by every repository (bound as a container singleton in the Univeros wiring, one per test case). A row already loaded returns the tracked instance; `UnitOfWork::clear()` drops the map. · Reason: matches Cycle's heap semantics the flows and tests relied on.
- 2026-09-09 · WP3 · `DatabaseAdapter` criteria are `column => scalar | list | null | Condition` (lt, lte, gt, gte, ne, notNull) and update data may carry `Increment`; the adapter boundary carries PHP scalars, null and `DateTimeImmutable`, adapters serialise per driver and return raw driver values that `RowMapper` hydrates by field type. `Dialect` gains a `Memory` case for the in-memory adapter. · Reason: the three compare-and-swap updates need `<` and `column + 1`, and pruning needs `IS NOT NULL` and `<`; everything else in the domain is equality, IN, IS NULL (verified in WP2). · Rejected: a query-builder-style `Where` object (spec's `coupling-analysis` sketch): more surface than the domain uses.
- 2026-09-09 · WP3 · The three compare-and-swap services (`OtpService`, `RecoveryCodeService`, `TokenService`) keep their shape: `?DatabaseAdapter $database = null` replaces `?ORMInterface $orm = null`, with the same entity-level fallback when null (the hand-written in-memory test doubles keep working). `PermissionCatalogSeeder` and `PruneExpiredService` take the adapter; each OR in pruning becomes two deletes. `Maintenance` moves to `Polaris\Maintenance`. · Rejected: moving CAS onto repository methods (would force concrete repository types on the services and rewrite nine unit tests).
- 2026-09-09 · WP3 · Schema definitions are generated once from the 1.0 entity attributes plus the index, unique and foreign-key list of the 18 migrations (generator kept out of the repository); `AuditLogRepository` keeps its 1.0 name for the `AuditLogEntry` model. Models keep their class names and defaults (`SchemaTest` checks every field maps both ways and every declared default equals the model's property default).
- 2026-09-09 · WP3 · The Univeros wiring and the Postgres suites stay alive on the new repositories through `Bootstrap\CycleDatabaseAdapter` (the Polaris adapter over Cycle's database, deleted in WP7; `Module::entityDirectories()` now returns nothing since Cycle no longer maps entities). The 17 `DatabaseTestCase` suites are re-based on the Polaris repositories now rather than in WP4, and `FunctionalTestCase` binds the adapter; no test is skipped. · Reason: spec ground rule 2 (green at every merged step) outweighs the throwaway glue. · Rejected: skipping the 41 database-backed test files until WP4/WP6.
- 2026-09-09 · WP3 · `packages/testing` ships `InMemoryAdapter` (criteria, ordering, limits, `Increment`, transactions that roll back on exceptions); the spec's fakes (`FakeMailer`, `FakeSms`, `FixedClock`, `RecordingDispatcher`) stay as the existing `tests/Support` doubles until WP6/WP7 need them outside core's tests. The conformance suite is a trait, `tests/AdapterConformance/AdapterConformanceTests`, mixed into one test case per adapter (in-memory now; SQLite and Postgres in WP4); its service-level flows (§4.6) are added in WP4 with the PDO adapter, while the functional suite covers them on Postgres meanwhile.
- 2026-09-09 · WP4 · `packages/pdo` ships `PdoAdapter` (PostgreSQL, MySQL, SQLite; prepared statements; savepoints for nested transactions; booleans bound as `PDO::PARAM_BOOL`, datetimes as `Y-m-d H:i:s`) and `SqlSchema`, the DDL exporter behind `schema:export` (`createAll`/`dropAll`/`create` per `Dialect`, referenced tables first). The `laravel`, `doctrine` and `cycle` export targets wait for the WP7 CLI; MSSQL is in the `Dialect` enum but untested. · Reason: spec §4.5 and §7.
- 2026-09-09 · WP4 · The 18 Cycle migrations are deleted (spec §1). `DatabaseTestCase` builds the schema from `SqlSchema` and seeds the permission catalog and system roles itself, which is what the seed migration did; `Module::migrationDirectories()` and `entityDirectories()` return nothing. Where deployments get their schema and seed is a WP7 question (`polaris schema:export` plus a seed command or first-boot seeding); noted for WP7.
- 2026-09-09 · WP4 · Local database tests run on in-memory SQLite by default (`DB_CONNECTION` unset); CI keeps PostgreSQL through the same env. The 1.0 rule "never SQLite" was about validating Cycle migrations against a production driver; with the schema exported per dialect, SQLite is the fast local loop and PostgreSQL the gate. The full suite drops from 21 minutes to about 75 seconds locally.
- 2026-09-09 · WP4 · Conformance: `SqliteAdapterConformanceTest` (explicit `sqlite::memory:`) always, `InMemoryAdapterConformanceTest` always, `DatabaseAdapterConformanceTest` on whatever the env selects (PostgreSQL on CI). The suite gained foreign-key-valid fixtures because SQLite enforces the keys the in-memory adapter ignores. The service-level flows of §4.6 stay covered by the functional suite, which now runs through the module wiring on the PDO adapter.
- 2026-09-09 · WP4 · The raw Cycle query-builder calls in 19 test files became adapter calls (`insert`, `findMany`, `count`, `update`, `delete`) plus `DatabaseTestCase` introspection helpers (`hasTable`, `hasColumn`, `hasIndex`, `primaryKey`) implemented per dialect; the migration rollback tests are replaced by `SqlSchemaTest` (create, drop, ordering, dialect types). `Bootstrap\CycleDatabaseAdapter` stays for a Univeros host but is no longer exercised by tests.
- 2026-09-09 · WP5 · Manifest validation is not on the request path. The YAML `rules` were documentation in 1.0 ("as the domain validates them"): nothing loaded them, and every endpoint validates by hand with its own 422 messages, which the contract freeze must keep. `Polaris\Http\Validation\{Rule, Rules}` parse and evaluate the ten rule forms for the manifest tests and the WP7 OpenAPI output; the endpoints stay the validators. · Rejected: spec §5.3's handler-level validation (would change every 422 body).
- 2026-09-09 · WP5 · One `Polaris\Http\Endpoint` base replaces the three 1.0 bases (`AuthDomain`, `OrganizationDomain`, `UserDomain`) with the union of their helpers; response envelopes are unchanged. Endpoints read request attributes by the names in `Polaris\Http\Attributes` (token, MFA ticket, authority, IP, user agent); `Input` merges route params, body, cookies, query and uploads in the framework's precedence, keeping attributes separate. The one endpoint serving two routes in 1.0 (`OtpFactorConfirmEndpoint`) stays shared; the manifest test allows exactly that list.
- 2026-09-09 · WP5 · The manifest loader (symfony/yaml, once per process) enforces the required keys and value sets itself; `api/schema.json` documents the format and a test keeps its `required` and `enum` lists equal to the loader's constants. · Rejected: a JSON-schema validator dependency (not in the §2 allow-list). Error entries may carry `code: null` for the one codeless 400 body 1.0 documents.
- 2026-09-09 · WP5 · Univeros glue for the port: `AltairEndpointBridge` (a framework domain wrapping an Endpoint, translating the framework's attribute names and rendering `Result` as a Payload) bound per manifest class under `<EndpointClass>@altair`; `Module::routes()` derives the table from the manifest; `Routes.php` is deleted. The authorization middleware maps the bridge id back to the endpoint class to read `REQUIRES_PERMISSIONS`.
- 2026-09-09 · WP5 · `bin/check-imports` is blocking for `Altair\` and `Cycle\` outside an explicit allow-list of the remaining glue (`Bootstrap/`, `Module.php`, `Http/Middleware/`, `Http/Rule/`, `Observability/MetricsListener.php`); WP6 removes the middleware entries, WP7 the rest. The acceptance line "grep Altair returns nothing" is met for every ported layer; the glue is what the stacked plan keeps alive until its WP.
- 2026-09-09 · WP6 · The container-free wiring (`Polaris\Wiring\{Config, Graph}`, `Polaris::create()`) is built in WP6 rather than WP7: the functional suite through PSR-15 needs the whole service graph without the framework container. `Graph` constructs every service explicitly in the order the six 1.0 binders used; endpoints are built from their typed constructor parameters against the graph's services (the three that take a generic repository are constructed explicitly), which replaces the container's autowiring without becoming one. WP7 keeps the CLI and the deletion of `Bootstrap/` and `Module.php`.
- 2026-09-09 · WP6 · `packages/psr15` routes with a `RouteMiddleware` first (the spec's `RequestHandler` also routes when run alone), so the middleware reads the matched `EndpointSpec` from `Attributes::ROUTE` and every policy comes from the manifest: `auth` (bearer / mfa_token), `rate_limit` group, `step_up`, and `REQUIRES_PERMISSIONS` from the spec's class. The 1.0 path-rule tables in `HttpBindings`/`MfaBindings` were verified equivalent to the spec values (the `/auth/mfa/factors` prefix covered the three factor routes the specs list; the six step-up routes match).
- 2026-09-09 · WP6 · Middleware order follows the 1.0 priorities, not the order spec §5.4 lists: ClientContext, AuthRateLimit, MfaToken, TokenAuthentication, StepUp, Denylist, AuthenticatedRateLimit, Authorization. · Reason: the contract fixtures show the per-user limiter wrapping authorization (a 403 carries `X-RateLimit-*` headers); the spec's listing was approximate.
- 2026-09-09 · WP6 · `RateStore` (§3.5) with `RateResult` and the PSR-16 `CacheRateStore` default reproduce the 1.0 fixed-window limiter and its headers; `MfaTicket` moves to `Polaris\Http` because `Endpoint::mfaTicket()` returns it. Core's `Http/Middleware` and `Http/Rule` stay (allow-listed) until WP7 deletes them with `Module.php`, which still registers them for a Univeros host; the psr15 package carries the ports.
- 2026-09-09 · WP6 · Contract freeze: 184 fixture files (1201 request/response steps, one per 1.0 functional test) recorded from the WP4 tree through the Univeros harness, replayed by the functional suite itself: `FunctionalTestCase::handle()` compares every response, normalised (uuids, JWTs, opaque tokens, secrets, QR images, recovery codes, datetimes, volatile rate-limit and Date headers, sorted keys), with the recorded step; `tearDown` asserts every recorded step was replayed; `ContractCoverageTest` proves the fixtures reach all 52 routes. A bare 404 the old harness produced for unknown routes is compared by status only (the PSR-15 handler answers the error envelope, spec §5.5). Deliberate changes go into `tests/Contract/KnownChanges` and `behaviour-changes.md`; there is one (the body-field attribute clobbering).
- 2026-09-09 · WP6 · The test and the application share one identity map (`FunctionalTestCase::boot()` adopts the graph's), as the Cycle heap was shared in 1.0; `boot()` rebuilds Polaris from the current environment so the tests that toggle flags keep calling `setUp()` again.
