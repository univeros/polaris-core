# Decisions log (task 2, framework adapters)

Append one entry per decision. Format: date, work package, decision, reason, alternatives rejected. Task 1's log is `../extraction/decisions.md`.

## Open at start (decide in the WP indicated)

- **WP1** — The transport headers Laravel's kernel adds to every response (the harness allow-list, spec §0.2): at least `cache-control`; the first functional run decides the exact list.
- **WP1** — Whether the Laravel migration runs inside the migrator's transaction (`PdoAdapter::transaction()` opens its own) or opts out.
- **WP2** — The Symfony authenticator's role mapping (`ROLE_POLARIS_<ROLE>`) and whether the organization goes into a token attribute or the user.
- **WP2** — Doctrine DBAL connection reuse (`getNativeConnection()`) versus a separate PDO from the DSN when both are configured.
- **WP3** — `yiisoft/db` connection reuse (`getPDO()`) versus a DSN.
- **WP4** — The npm scope (`@polaris-auth/*` is the placeholder from `target-design.md` §9); the owner's call.
- **Any** — PHP minimum stays 8.3.

## Log

<!-- YYYY-MM-DD · WPn · decision · reason · rejected alternatives -->
- 2026-09-10 · WP0 · Task 2 is the three framework adapters plus the TypeScript client; the Agents and Cloud plugins are task 3 because they need the plugin contract (`target-design.md` §3.2), which is feature work on core with its own spec. · Reason: the extraction spec §10 lists both groups as "next tasks"; the adapters need no core feature, the plugins need one.
- 2026-09-10 · WP0 · Each adapter uses the framework's database connection through `polaris/pdo` (the PDO handle the framework already holds); no `polaris/eloquent` or `polaris/doctrine` adapter is built in this task. · Reason: `PdoAdapter` is what the conformance suite and the contract freeze run on; a second SQL path per framework would double the proof surface for no behaviour. · Rejected: dedicated ORM adapters (target-design §1 lists them; they wait for a need).
- 2026-09-10 · WP0 · The proof of an adapter is the unchanged functional suite plus the 184 fixtures through the framework's real HTTP kernel, selected by `POLARIS_HARNESS` (spec §3.7), with an explicit per-adapter allow-list of transport headers the framework adds to every response. · Reason: "no adapter changes a response" has to be checked by the same comparison that froze the contract; the allow-list keeps the framework's transport layer out of it without weakening the rest. · Rejected: a second, adapter-specific fixture set (would drift); stripping the framework's headers in the adapter (fighting the framework for the test's sake).
- 2026-09-10 · WP0 · Schema in a framework: the adapter's migration or command runs `SqlSchema` for the connection's dialect and seeds the catalog; no translation into Blueprint, Doctrine schema or Yii migrations. · Reason: one source of DDL, proven by `schema:diff`; a translation would be a second schema to keep equal. · Rejected: `schema:export --target=laravel|doctrine` emitting builder code (task 1 left the targets to the adapters; this decides what they are).
- 2026-09-10 · WP1 · Laravel routes: one named Laravel route per manifest endpoint (`polaris.auth.login`, ..., `PolarisController` for all), no catch-all. · Reason: a catch-all under `path_prefix: /` matches everything registered after it and shadows the application's own routes; per-endpoint routes give `route:list` and `route()` for free and leave unknown paths to the application's 404. The spec's §3.2 is amended. · Rejected: a catch-all route (shadowing); deferring the catch-all to `booted` (order-dependent).
- 2026-09-10 · WP1 · What Laravel's transport adds and the contract comparison ignores: the `cache-control` header (HttpFoundation computes `no-cache, private` for every response without one), declared by `Harness::transportHeaders()`; and the `Content-Type: text/html; charset=utf-8` HttpFoundation sets on a body-less response Polaris sent without one (the rate limiter's 429), which the harness removes before comparing, because PHP's SAPI sends that same default from any host and the recorded fixtures never saw a wire. Everything else is byte-identical: 184 fixtures, 1,201 steps, through the HTTP kernel, on SQLite locally and PostgreSQL in CI.
- 2026-09-10 · WP1 · The generated migration runs with `$withinTransaction = false`. · Reason: `PdoAdapter::transaction()` opens its own transaction on the same PDO handle (the seeder runs inside one); nested inside the migrator's it would fail with "There is already an active transaction". Rollback is `SqlSchema::dropAll()`. (Closes the WP1 open item.)
- 2026-09-10 · WP1 · Secrets: `config/polaris.php` reads `POLARIS_APP_KEY` and falls back to Laravel's `APP_KEY` (`base64:` plus 32 bytes, so at least 32 bytes long); every Polaris key is HKDF-derived under its own context, so sharing the master key with Laravel's encrypter exposes nothing. PEM keys come from `AUTH_JWT_*_FILE` paths (relative to the base path) or the `AUTH_JWT_*` values.
- 2026-09-10 · WP1 · Core friction fixed in `polaris/cli`: `SchemaDiffCommand` and `DoctorCommand` take optional providers (a connection, the `Secrets` and the `AuthConfig`) so a host's console can run them on its own configuration; without them they read `--dsn`/`POLARIS_DSN` and the environment as before. `Database::dialectOf(PDO)` detects the dialect of a handle. The four commands appear in artisan as `polaris:schema:export`, `polaris:schema:diff`, `polaris:manifest`, `polaris:doctor` (renamed with `setName()`, added through `Artisan::starting`, as plain Symfony commands).
- 2026-09-10 · WP1 · `symfony/console`, `symfony/yaml` and `symfony/uid` constraints widen to `^7.0 || ^8.0` in `polaris/cli` and `polaris/core`. · Reason: Laravel 13 accepts Symfony 8, and an application on PHP 8.4 with Symfony 8 could not otherwise require `polaris/cli`; the root keeps resolving 7.4 through its 8.3 platform.
- 2026-09-10 · WP1 · The harness (`Polaris\Laravel\Tests\Harness`) boots the `orchestra/testbench-core` skeleton with the provider, hands the test's instances (adapter, mailer, SMS sender, secrets, auth, and the recording dispatcher) to `config('polaris')`, which accepts objects, serialises the PSR-7 request to bytes (Laravel parses JSON from the raw body), runs `Kernel::handle()` and converts back. The functional run therefore proves routing, request and response conversion and the provider's wiring; the event bridge, the mail bridge, the guard and the install command have unit tests. One application lives at a time (the previous is flushed, PHP's handlers restored), so the 185 tests fit in 45 MB.
- 2026-09-10 · WP1 · `mailer: mail` is a plain-text bridge over Laravel's mailer with one view per common template and a generic fallback (`polaris::mail.notification`), subjects in the class; the default stays `log`, as in core. · Rejected: HTML templates and a URL builder (the application knows its front end, Polaris does not).
- 2026-09-10 · WP1 · PHPUnit suites: `core` (everything but Functional and Contract), `functional` (Functional plus Contract), `laravel`; `composer test:laravel` runs the functional suite with `POLARIS_HARNESS` set and is part of `composer qa`; CI runs it in the QA matrix on PostgreSQL and adds a `demo-laravel` job. `bin/check-imports` takes an optional root so its rules are unit-tested; `Symfony\Component\HttpFoundation` is allowed in `packages/laravel` because Laravel is built on it.
- 2026-09-10 · WP2 · The bundle wires Polaris entirely in the container: `Polaris\Wiring\Config` is a service whose arguments are the `polaris:` configuration (`Secrets` from values or PEM files through `Factory::secrets()`, `AuthConfig::fromArray()`, `RateLimitConfig::fromArray()`) and service references (`polaris.database` from a DSN or from the application's connection service, a PDO, a Doctrine DBAL connection or a Polaris adapter; `polaris.cache` wrapping a PSR-6 pool in `Psr16Cache`; `logger`; `event_dispatcher`); `Polaris::create()` is the factory of the `Polaris` service. Every value object also accepts `service: id`, so a host (and the harness) can hand over instances. · Reason: no PolarisFactory class to test separately; DI does the composition and the compiled container is the proof. · Rejected: a runtime factory reading the container (Laravel needed one because its config holds objects; Symfony's does not).
- 2026-09-10 · WP2 · Routes: the `polaris` route loader (`type: polaris`) yields one named route per manifest endpoint under `polaris.path_prefix`, the same names as in Laravel (`polaris.auth.login`); the prefix is the bundle's setting, not the import's `prefix:`, because the pipeline's router must strip the same prefix. Unknown paths are the application's 404, a wrong method on a Polaris route is Polaris's 405.
- 2026-09-10 · WP2 · Symfony's dispatcher is PSR-14 but dispatches by exact class, so `PolarisEventSubscriber` subscribes the Polaris listeners to every class in `Polaris\Event` (34, found by listing the directory at compile time); the application's own listeners for those classes keep working. · Rejected: a fixed list of 34 class names (drifts).
- 2026-09-10 · WP2 · The authenticator (`custom_authenticators`) is also the firewall's entry point: a missing or invalid bearer answers 401 `{"error":"unauthorized"}` with `WWW-Authenticate: Bearer`, the same envelope Polaris routes use, instead of Symfony's default (a 401 without body or a redirect). No user provider is needed: the `UserBadge` loads the user through `Graph::users()`. Roles: `ROLE_USER` plus `ROLE_POLARIS_<ROLE>` from the token's `roles` claim; the organization stays a claim (`PolarisUser::claim('org')`). (Closes the WP2 open items on roles and the organization.)
- 2026-09-10 · WP2 · What Symfony's transport adds and the harness handles: `cache-control` (HttpFoundation, as in Laravel), the same body-less `Content-Type` case, and `X-Robots-Tag: noindex`, which FrameworkBundle adds to every response in debug mode (`disallow_search_engine_index` defaults to the debug flag); the test kernel sets it to false because a production kernel does not send it. 184 fixtures, 1,201 steps, identical.
- 2026-09-10 · WP2 · Schema: `polaris:schema:create` and `polaris:schema:drop` run `Polaris\Pdo\SchemaInstaller` (new in `polaris/pdo`: the SQL exporter for the connection's dialect plus the seeder, or the drop statements) on the bundle's PDO; the Laravel `PolarisSchema` now delegates to it. No Doctrine Migrations integration: a host that wants one calls the installer from a migration class. · Reason: one implementation for every adapter; the core friction fix is logged here.
- 2026-09-10 · WP2 · The harness boots a `TestKernel` (MicroKernelTrait; FrameworkBundle, the PolarisBundle; SecurityBundle only for the authenticator tests) whose compiled container is cached per configuration hash in the temporary directory, hands the test's instances over as synthetic public services (`polaris.test.*`) the bundle's `service:` keys point at, pushes each request through `Kernel::handle()` as bytes and converts the response back; the previous kernel is shut down when the next test boots. The `mailer: mail` bridge is a plain-text `Symfony\Component\Mailer` client with `mail_from`; `symfony/mailer` is a suggestion of the package and a dev requirement of the root. Doctrine DBAL is used through `getNativeConnection()` when configured, never required. (Closes the WP2 DBAL open item.)
- 2026-09-10 · WP2 · Symfony resets every service tagged `kernel.reset` between the requests one process handles, and its array cache adapter is one of them, so with `cache.adapter.array` the rate limiter, the denylist and the OTP send cap started every request empty (three fixtures failed). The harness hands the test's own PSR-16 cache (core's `InMemoryCache`) to the bundle as a synthetic service, as it hands the database; a production `cache.app` (filesystem, Redis) persists. The bundle README says so: Polaris needs a cache that outlives a request. · Rejected: disabling the services resetter in the test kernel (hides the property a host must know about).
- 2026-09-10 · WP3 · The Yii adapter is a `yiisoft/config` plugin: `params` (the `polaris` tree of spec §3.1 plus the `yiisoft/yii-console` command map), `di` (the value objects, the PDO from `database.dsn` or the application's PDO / `Yiisoft\Db` connection, `Config`, `Polaris`, `Graph`, `Pipeline`, the authentication method and the `polaris/authentication` middleware), `di-console` (the CLI commands with `setName()` calls, because Yii's command loader registers each under the map's key), `routes` (one named route per manifest endpoint under `path_prefix`, PSR-15 straight through: no bridge, no transport headers) and `events-web`/`events-console` (`PolarisListener` on every `Polaris\Event` class). The application provides what every Yii application has (PSR-17 factories, a PSR-16 cache, a logger, `yiisoft/yii-event`, the route collection and the HTTP application), and `Factory::config()` takes them from the container. · Reason: the Yii way is configuration groups, not a provider class. · Rejected: a `ServiceProviderInterface` (bypasses the config plugin the ecosystem is built on).
- 2026-09-10 · WP3 · `schema:create` and `schema:drop` live in `polaris/cli` (`--dsn` or a host's connection, on `SchemaInstaller`), so `bin/polaris`, Symfony and Yii share one implementation; the Symfony copies from WP2 are removed. Plain-text mail subjects and bodies live in core as `Polaris\Notification\MailTemplates`, used by the Symfony and Yii bridges (Laravel keeps its publishable Blade views). · Reason: three copies of the same text was the wrong shape after the second adapter.
- 2026-09-10 · WP3 · A wrong method on a Polaris route answers Yii's router 405 (empty body, `Allow` header) rather than Polaris's envelope, because `yiisoft/router` matches the path before the action runs; no fixture covers a 405, and the application's 404 is Yii's `NotFoundHandler`. The harness's transport allow-list is empty: Yii adds nothing to a response.
- 2026-09-10 · WP3 · The client IP: `yiisoft/proxy-middleware`'s `requestClientIp` attribute is honoured when present; without it `ClientContextMiddleware` reads `REMOTE_ADDR`, as in the other hosts.
- 2026-09-10 · WP4 (owner) · A `polaris/univeros` adapter joins task 2 as WP4, ahead of the TypeScript client (now WP5); `univeros/polaris` 2.0 becomes a wrapper (a meta-package requiring `polaris/univeros`), 1.x installations are not affected, 2.0 is announced as a breaking change with no data-compatibility layer; migrations go through a Cycle migration directory calling `SchemaInstaller` so `bin/altair db:migrate` works; the routes' integration is left to the adapter's author. · Reason: the owner's answers of 2026-09-10 to the five open points (package name and fate of `univeros/polaris`, routes, 1.0 data, migrations, order).
- 2026-09-10 · WP4 · Routes in Univeros: `PolarisMiddleware` first in the Relay pipeline, ahead of the framework's `ExceptionHandlerMiddleware` (which rewrites every error response into problem+json), serving every manifest path through `Pipeline::handle()`; the Polaris routes are not registered in the FastRoute table because they would never be dispatched. · Reason: the contract must survive the framework's error rewriting, and a route that is never dispatched misleads `routes:list`. · Rejected: per-endpoint bridge Actions in the ADIR lifecycle (the 1.0 shape) inside the exception handler (breaks the envelopes); a custom status validator on the exception handler (it sees status codes, not routes).
- 2026-09-10 · WP4 · The namespace ban becomes the adapter rule: `Altair\`, `Cycle\` and `Univeros\` allowed only under `packages/univeros` and `examples/univeros`; every other package stays free of them (the point of the extraction).
